{
  "name": "hardhat-project",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^1.0.2",
    "hardhat": "^2.13.0"
  },
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test"
  }
}
